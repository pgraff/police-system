spring:
  application:
    name: call-projection-test
  kafka:
    consumer:
      group-id: call-projection-test-group
      auto-offset-reset: earliest
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql

projection:
  nats:
    enabled: true

