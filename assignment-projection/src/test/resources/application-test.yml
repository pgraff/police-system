spring:
  application:
    name: activity-projection-test
  kafka:
    consumer:
      group-id: activity-projection-test-group
      auto-offset-reset: earliest
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql

projection:
  nats:
    enabled: true

