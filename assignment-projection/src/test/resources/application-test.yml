spring:
  application:
    name: assignment-projection-test
  kafka:
    consumer:
      group-id: assignment-projection-test-group
      auto-offset-reset: earliest
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql

projection:
  nats:
    enabled: true
