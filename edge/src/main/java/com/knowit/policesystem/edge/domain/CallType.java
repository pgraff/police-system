package com.knowit.policesystem.edge.domain;

/**
 * Type values for calls.
 * Matches the CallType enum in the OpenAPI specification.
 */
public enum CallType {
    Emergency,
    NonEmergency,
    Administrative,
    Other
}
