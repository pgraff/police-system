package com.knowit.policesystem.edge.domain;

/**
 * Type values for vehicles.
 * Matches the VehicleType enum in the OpenAPI specification.
 */
public enum VehicleType {
    Patrol,
    SUV,
    Motorcycle,
    Van,
    Truck
}
