package com.knowit.policesystem.edge.domain;

/**
 * Type values for assignments.
 * Matches the AssignmentType enum in the OpenAPI specification.
 */
public enum AssignmentType {
    Primary,
    Backup,
    Supervisor,
    Other
}
