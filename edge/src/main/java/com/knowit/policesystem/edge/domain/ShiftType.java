package com.knowit.policesystem.edge.domain;

/**
 * Type values for shifts.
 * Matches the ShiftType enum in the OpenAPI specification.
 */
public enum ShiftType {
    Day,
    Evening,
    Night,
    Other
}
