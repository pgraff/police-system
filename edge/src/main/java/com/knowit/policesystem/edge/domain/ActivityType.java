package com.knowit.policesystem.edge.domain;

/**
 * Type values for activities.
 * Matches the ActivityType enum in the OpenAPI specification.
 */
public enum ActivityType {
    Arrest,
    Interview,
    Evidence,
    Report,
    Other
}
