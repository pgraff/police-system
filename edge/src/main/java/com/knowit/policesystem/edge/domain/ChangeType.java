package com.knowit.policesystem.edge.domain;

/**
 * Change type values for shift changes.
 * Matches the ChangeType enum in the OpenAPI specification.
 */
public enum ChangeType {
    Briefing,
    Handoff,
    Status,
    Other
}
