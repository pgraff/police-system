package com.knowit.policesystem.edge.domain;

/**
 * Types of incidents.
 * Matches the IncidentType enum in the OpenAPI specification.
 */
public enum IncidentType {
    Traffic,
    Theft,
    Assault,
    Domestic,
    Burglary,
    Other
}
