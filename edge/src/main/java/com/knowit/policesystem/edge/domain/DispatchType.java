package com.knowit.policesystem.edge.domain;

/**
 * Type values for dispatches.
 * Matches the DispatchType enum in the OpenAPI specification.
 */
public enum DispatchType {
    Initial,
    Additional,
    Cancellation,
    Other
}
