package com.knowit.policesystem.edge.domain;

/**
 * Priority levels for incidents and calls.
 * Matches the Priority enum in the OpenAPI specification.
 */
public enum Priority {
    Emergency,
    High,
    Medium,
    Low
}
