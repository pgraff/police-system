spring:
  application:
    name: dispatch-projection-test
  kafka:
    consumer:
      group-id: dispatch-projection-test-group
      auto-offset-reset: earliest
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql

projection:
  nats:
    enabled: true

